@model IEnumerable<test_orcl.Models.BTDBenhNhan>
@{
    ViewBag.Title = "Thông tin hành chánh";
    Layout = "";
}
@*

    @if (Model != null && Model.Any())
    {
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading font-weight-bold" style="color:darkred">
                        Thông tin hành chánh
                    </div>
                    @foreach (var item in Model)
                    {
                        <div class="panel-body">

                            <div class="row">

                                <div class="col-lg-6">
                                    <form role="form">

                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Mã bệnh nhân:</label>
                                            <div class="col-sm-9">
                                                <input type="text" class="form-control" value="@item.MABN" readonly />
                                            </div>
                                        </div>
                                        .
                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Họ tên:</label>
                                            <div class="col-sm-9" style="">
                                                <input type="text" class="form-control" value="@item.HOTEN" readonly />
                                            </div>
                                        </div>
                                        .
                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Năm sinh:</label>
                                            <div class="col-sm-9">
                                                <input type="text" class="form-control" value="@item.NAMSINH" readonly />
                                            </div>
                                        </div>
                                        .
                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Giới tính:</label>
                                            <div class="col-sm-9 mb-3">
                                                <input type="text" class="form-control" value="@item.PHAI" readonly />
                                            </div>
                                        </div>
                                    </form>
                                </div>

                                <div class="col-lg-6">
                                    <form role="form">

                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Địa chỉ:</label>
                                            <div class="col-sm-9 mb-3">
                                                <input type="text" class="form-control" value="@item.DIACHI" readonly />
                                            </div>
                                        </div>
                                        .
                                        <div class="form-group">
                                            <label class="col-sm-3 control-label">Số điện thoại:</label>
                                            <div class="col-sm-9">
                                                <input type="text" class="form-control" value="@item.DIDONG" readonly />
                                            </div>
                                        </div>

                                    </form>
                                </div>
                            </div>
                            <!-- /.row (nested) -->
                        </div>
                        <!-- /.panel-body -->
                    }
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    }
    else
    {
        <p>Không có thông tin người bệnh.</p>
    }

*@

@if (Model != null && Model.Any())
{
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading font-weight-bold" style="color:darkred">
                    Thông tin hành chánh
                </div>
                @foreach (var item in Model)
                {
                    <div class="panel-body">
                        <div class="row">
                            <!-- Cột thông tin bệnh nhân -->
                            <div class="col-lg-6">
                                <form role="form">
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Mã bệnh nhân:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.MABN" readonly />
                                        </div>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Họ tên:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.HOTEN" readonly />
                                        </div>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Năm sinh:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.NAMSINH" readonly />
                                        </div>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Giới tính:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.PHAI" readonly />
                                        </div>
                                    </div>
                                </form>
                            </div>
                            <!-- Cột địa chỉ và số điện thoại -->
                            <div class="col-lg-6">
                                <form role="form">
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Địa chỉ:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.DIACHI" readonly />
                                        </div>
                                    </div>
                                    <div class="form-group row">
                                        <label class="col-sm-4 control-label">Số điện thoại:</label>
                                        <div class="col-sm-8">
                                            <input type="text" class="form-control" value="@item.DIDONG" readonly />
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
}
else
{
    <p class="text-center text-warning">Không có thông tin người bệnh.</p>
}
